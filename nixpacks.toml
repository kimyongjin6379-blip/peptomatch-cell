[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "sqlite"]

[phases.install]
cmds = [
    "python -m venv --copies /opt/venv",
    "/opt/venv/bin/pip install --upgrade pip",
    "/opt/venv/bin/pip install -r requirements.txt",
    "/opt/venv/bin/pip install -e . || echo 'editable install skipped'",
    "chmod +x start.sh",
    "mkdir -p /app/_build_data && cp -rv data /app/_build_data/ 2>/dev/null || true",
]

[start]
cmd = "bash start.sh"
